/**
 * Main entry point of the application.
 * Currently only creates the MinefieldViewer class which acts as the
 * Controller in this pseudo MVC model.
 *
 * @author Samuel Klein
 */

import "./css/style.css";
import MinefieldViewer from "./MinefieldViewer";

new MinefieldViewer();